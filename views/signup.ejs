<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>Signup</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css"/>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %> 
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <li> <%= messages.error[i] %></li>
                <% } %>
              <% } %>
            <h6 class="py-4 font-bold">Signup as a new user</h6>
            <form action="/users" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken%>">
                <div class="py-3 font-bold">
                    <label for="firstName">First Name:</label>
                    <input type="text" name="firstName" class="border border-gray-400 font-medium p-0.5 w-full rounded">
                </div>
                <div class="py-3 font-bold">
                    <label for="lastName">Last Name:</label>
                    <input type="text" name="lastName" class="border border-gray-400 font-medium p-0.5 w-full rounded">
                </div>
                <div class="py-3 font-bold">
                    <label for="email">Email:</label>
                    <input type="email" name="email" class="border border-gray-400 font-medium p-0.5 w-full rounded">
                </div>
                <div class="py-3 font-bold">
                    <label for="password">password:</label>
                    <input type="password" name="password" class="border border-gray-400 font-medium p-0.5 w-full rounded">
                </div>
                <div class="py-3 font-bold">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-bold rounded px-2 py-2">Sign up</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>